{
  "name": "solidfi",
  "version": "0.1.0",
  "description": "Composable type-safe conversion framework",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^18.19.130",
    "tsx": "^4.23.1",
    "typescript": "^5.4.0"
  }
}
